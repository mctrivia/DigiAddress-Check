#source for libraries.min.js

On Windows computers run ~build.bat to rebuild the libraries.min.js from original source code.

Libraries Used: 
-iancoleman's bip39 tool: Used to calculate key path for seeds
